/** Espelha `CharacterLayers` do frontend (frontend/src/api/character.ts). */
export interface CharacterLayers {
  skin: string;
  eyes: string;
  hair: string;
  clothes: string;
  accessory: string;
}
